{
  "name": "logviewer",
  "version": "0.0.3",
  "scripts": {
    "build": "npm install && bower install"
  },
  "devDependencies": {
    "express": "3.x",
    "socket.io": "~0.9.0",
    "tail": "~0.3.1"
  },
  "engines": {
    "node": ">=0.8.0"
  }
}